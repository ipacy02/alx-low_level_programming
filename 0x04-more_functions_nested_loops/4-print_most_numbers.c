#include "main.h"
/**
 * print_most_numbers -print that
 * Return: number to not be printed
 */
void print_most_numbers(void)
{
	int c;
	
	for (c == 0 && c <= 9 && c != 2 && c != 4)
	{
		_putchar(c + '0');
}
_putchar('\n');
}
