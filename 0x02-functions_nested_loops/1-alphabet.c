#include "main.h"

/**
 * print_alphabet -we are printing lowecase
 * Return: void to the output
 */

void print_alphabet(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
		_putchar (c);
	_putchar ('\n');
}
