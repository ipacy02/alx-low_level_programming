#include "main.h"
/**
 * _islower - checks for lowercase character
 * @c: c is the character we can enter and use to check
 * Return: 1 if c is lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}

